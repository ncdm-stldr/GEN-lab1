Here is the list of git commands used to create the workflow (not necessary complete yet):

#initialization phase
git init
git remote add origin git@github.com:ncdm-stldr/GEN-lab1.git

#related to commit n°1 of branch master
git add Readme.md commands.txt
git commit -m "commit 1 on master"
git push origin master